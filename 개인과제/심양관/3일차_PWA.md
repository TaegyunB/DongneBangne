
## PWA(Progressive Web App)란 무엇인가?
카톡, 인스타그램 등의 앱들은 대부분 네이티브 앱이다. 하지만 앱까지 따로 설치하지 않고도, 웹에서 앱처럼 쓸 수 없을까? 라는 고민에 대해서 다양한 대답 중 하나가 PWA(Progressive Web App)이다. 즉, 웹 앱인데 네이티브 앱 같은 기능을 제공하는 웹 기술이다. 

### Progressive의 의미
PWA는 어떤 환경에서든 최선을 다한다라는 철학을 가지고 있다. 사용자의 브라우저가 최신이면 최신일수록 더 풍부한 기능(오프라인, 푸시, 설치 등)을 제공하고, 브라우저가 옛날 것이라면 최소한의 웹 사이트 기능만 제공한다. 이에 점진적으로 향상 => (progressive enhencement)라고 부른다. 

### PWA의 특징
- **설치 가능**
  - 사용자가 웹사이트를 모바일이나 PC의 홈 화면에 앱 아이콘으로 추가할 수 있다. 
  - 네이티브 앱처럼 별도의 스토어에서 다운로드할 필요가 없다.
- **오프라인에서도 작동 (offline capable)**
  - 서비스 워커(Service Worker) 덕분에, 한 번 방문한 페이지는 캐시에 저장돼서 인터넷이 끊겨도 동작한다.
  - 느린 네트워크 환경에서도 앱처럼 빠르게 열린다.
- **푸시 알림**
  - 네이티브 앱처럼 사용자에게 푸시 메시지를 보낼 수 있다.
- **HTTPS 필수**
  - HTTPS를 반드시 사용해야 서비스워커가 등록되고, PWA가 정상 작동한다.
  - 보안(중간자 공격 방지)도 기본으로 강화된다. 
- **반응형**
  - 스마트폰, 태블릿, PC 등 어떤 디바이스에서도 화면이 깨지지 않고 유연하게 작동한다.
- **검색 및 공유**
  - 앱스토어 링크 없이도 URL 하나만 복사해 보내면 상대방은 바로 브라우저에서 동일 앱을 사용할 수 있다. 

### PWA의 장점
- **Single codebase**
  - iOS, Android, PC 각각 따로 개발할 필요 없이 웹 기술 하나(HTML, CSS, JS)로 모두 커버할 수 있다.
  - 유지보수에 유리하다.
- **빠른 로딩, 끊김 없는 경험**
  - 캐시와 서비스 워커 덕분에 두 번째 방문부터는 로딩이 즉각적이다.
  - 오프라인에서도 동작해 사용자 경험이 탁월하다.
- **푸시 알림 + 설치**
  - 웹임에도 푸시 알림을 활용할 수 있다.
  - 사용자가 홈 화면에 설치할 수 있어 앱처럼 보이며, 실행도 네이티브 앱처럼 빠르다.

### PWA의 단점
- **하드웨어 연동은 제한적**
  - NFC, 블루투스, 진동, 근접 센서 같은 기능은 네이티브 SDK보다 제한적이다.
  - 더 많은 API가 나오고 있으나 아직 완전하지 않다.
- **브라우저마다 호환성 편차**
  - iOS Safari는 한동안 푸시 알림조차 지원하지 않아 불만이 있었고, 최근에야 조금씩 개선되고 있다. 아직도 플랫폼별 기능 차이가 존재한다. 

---

### 한 줄 요약
> **PWA는 “웹이지만 앱처럼 작동하는 새로운 웹 앱 형태”로, 설치, 오프라인, 푸시를 갖춘 가장 진화된 웹 애플리케이션이다.**

---

## PWA를 위한 Figma 와이어프레임 기본 가이드
1. **기본 프레임**
   - Mobile-first 접근을 권장. 
   - iPhone13 (390×844), Pixel5 (393×851), iPhone X~13 (375×812) 사이즈가 보편적.
   - PWA는 모바일 홈화면 설치를 주로 고려하기 때문에 모바일에서 시작 후 반응형으로 확장하는 것이 유리하다.

2. 오프라인 화면이 필요하면 별도 설계
3. 푸시 알림 권한 UX를 적절한 흐름에 삽입
4. 전체 화면(notch 포함) 여백 & theme-color 시뮬레이션

---

### 참고: 다른 서비스 UX 비교
| 항목               | Twitter Lite                  | Starbucks PWA                    |
|--------------------|-------------------------------|----------------------------------|
| **설치 유도 UX**   | 배너 + CTA                    | 모달 + CTA                       |
| **푸시 흐름**      | 메시지 권한 팝업              | 주문 알림 + 권한 흐름 포함       |
| **오프라인 대응**  | 타임라인 캐싱, retry 팝업     | 메뉴 브라우징 캐시 + fallback    |
| **네비게이션**     | 주소창 없는 전체 화면         | 모바일 푸터 메뉴, 데스크탑 여백 |
| **로딩 UX**       | 간단 Loader                   | Skeleton + 스플래시              |

---

## PWA 프론트엔드의 필요 기술 역량

### 1. Service Worker
- 브라우저에 별도로 등록되는 JS 스크립트
  - 오프라인에서도 동작
  - 캐싱 전략을 제어
  - 푸시 알림 처리
- `register()`, `install`, `activate`, `fetch` 직접 다루거나 Workbox로 효율적 관리

**Vue 3에서는**
- `vite-plugin-pwa` 또는 `@vue/cli-plugin-pwa`를 설치하면 서비스워커를 자동으로 생성/관리
- 내부적으로 Workbox 사용 → cacheFirst, networkFirst, staleWhileRevalidate 전략 쉽게 적용

```javascript
npm install vite-plugin-pwa --save-dev

// vite.config.js 예시
import { VitePWA } from 'vite-plugin-pwa'

export default {
  plugins: [
    VitePWA({
      registerType: 'autoUpdate',
      workbox: {
        runtimeCaching: [
          {
            urlPattern: /^https:\/\/api\.example\.com/,
            handler: 'NetworkFirst',
            options: { cacheName: 'api-cache' }
          }
        ]
      }
    })
  ]
}
```

---

### 2. manifest.json & install UX
- 앱 아이콘, 이름, 테마색, 화면 모드 등 정의
- 홈 화면 설치 시 보이는 요소 설정
- `beforeinstallprompt` 이벤트로 설치 모달 UX 설계 가능

```javascript
onMounted(() => {
  window.addEventListener('beforeinstallprompt', (e) => {
    e.preventDefault()
    myInstallPrompt.value = e // 나중에 showInstallPrompt()에서 e.prompt()
  })
})
```

---

### 3. 오프라인 & 네트워크 UX
- `navigator.onLine`과 `online` / `offline` 이벤트로 상태 관리
- `v-if`로 fallback UI 구성

```javascript
const isOnline = ref(navigator.onLine)

onMounted(() => {
  window.addEventListener('online', () => isOnline.value = true)
  window.addEventListener('offline', () => isOnline.value = false)
})
```

```html
<div v-if="!isOnline">😢 인터넷이 없어요. 다시 연결해 주세요.</div>
```

---

### 4. 정리
| PWA 기능                | Vue 3 해소 방식                                      |
|--------------------------|----------------------------------------------------|
| 서비스워커 & 캐싱         | `vite-plugin-pwa`, `@vue/cli-plugin-pwa` + Workbox |
| manifest.json & install  | plugin 자동 생성 + `beforeinstallprompt` 직접 UX  |
| 오프라인 & 네트워크 UX    | Composition API + `navigator.onLine`               |
| 푸시 알림 & Notification | 브라우저 API + 서비스워커 직접 핸들링              |
| Lighthouse & HTTPS       | vite build + Lighthouse + mkcert HTTPS             |
